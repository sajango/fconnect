version: '3'

volumes:
  fconnect_app_local_postgres_data: {}
  fconnect_app_local_postgres_data_backups: {}

services:
  django: &django
    build:
      context: .
      dockerfile: ./compose/local/django/Dockerfile
    image: fconnect_app_local_django
    container_name: fconnect_app_local_django
    platform: linux/x86_64
    depends_on:
      - postgres
      - redis
    volumes:
      - .:/app:z
    env_file:
      - ./.envs/.local/.django
      - ./.envs/.local/.postgres
    ports:
      - "8000:8000"
    command: /start

  postgres:
    build:
      context: .
      dockerfile: ./compose/production/postgres/Dockerfile
    image: fconnect_app_production_postgres
    container_name: fconnect_app_local_postgres
    volumes:
      - fconnect_app_local_postgres_data:/var/lib/postgresql/data:Z
      - fconnect_app_local_postgres_data_backups:/backups:z
    env_file:
      - ./.envs/.local/.postgres

  docs:
    image: fconnect_app_local_docs
    container_name: fconnect_app_local_docs
    platform: linux/x86_64
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - ./.envs/.local/.django
    volumes:
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./fconnect_app:/app/fconnect_app:z
    ports:
      - "9000:9000"
    command: /start-docs

  redis:
    image: redis:6
    container_name: fconnect_app_local_redis

  celeryworker:
    <<: *django
    image: fconnect_app_local_celeryworker
    container_name: fconnect_app_local_celeryworker
    depends_on:
      - redis
      - postgres
    ports: []
    command: /start-celeryworker

  celerybeat:
    <<: *django
    image: fconnect_app_local_celerybeat
    container_name: fconnect_app_local_celerybeat
    depends_on:
      - redis
      - postgres
    ports: []
    command: /start-celerybeat

  flower:
    <<: *django
    image: fconnect_app_local_flower
    container_name: fconnect_app_local_flower
    ports:
      - "5555:5555"
    command: /start-flower
